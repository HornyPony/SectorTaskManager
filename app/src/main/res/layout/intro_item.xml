<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.makeramen.roundedimageview.RoundedImageView
        android:id="@+id/guideImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_launcher_background"
        app:riv_corner_radius="@dimen/guideImageRadius"
        android:adjustViewBounds="true" />

    <TextView
        android:id="@+id/slideNumberTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="FIRST SLIDE"
        android:textSize="@dimen/slideNumberTextSize"
        android:layout_below="@+id/guideImageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/defaultTopMargin"
        />

    <TextView
        android:id="@+id/guideDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="Tap here"
        android:textSize="@dimen/guideDescriptionSize"
        android:layout_below="@+id/slideNumberTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/defaultTopMargin"
        />

</RelativeLayout>